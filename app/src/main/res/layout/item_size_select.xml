<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.cardview.widget.CardView
        android:id="@+id/card_view_size_select"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/dp_10"
        android:backgroundTint="@color/activity_home_bg"
        app:cardCornerRadius="@dimen/dp_8"
        app:cardElevation="@dimen/dp_0"
        tools:context=".CatalogueFragment">

        <ImageView
            android:id="@+id/btn_size_select"
            android:layout_width="@dimen/dp_47"
            android:layout_height="@dimen/dp_47"
            android:background="@color/white"
            app:backgroundTint="@null" />

        <TextView
            android:id="@+id/txt_color_select"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:fontFamily="@font/sf_pro_text_regular"
            android:text="@string/TxtDemoSize"
            android:textColor="@color/dark_gray"
            android:textSize="@dimen/sp_14" />
    </androidx.cardview.widget.CardView>
</layout>

